/target

# Machine-specific MSVC/SDK overrides for Windows; regenerate with
# scripts/setup-windows-env.ps1 after cloning.
/.cargo/config.toml
*example*

# Local env files (never commit secrets).
.env
.env.*
!.env.example